// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

/**
 * @title IDomaProtocol
 * @dev Minimal placeholder interface for Doma Protocol integration hooks.
 * Projects can wire concrete contracts here when building full integrations.
 */
interface IDomaProtocol {
    // Example hook signatures (intentionally not enforced in this hackathon scope)
    // function notifyVaultEvent(address vault, bytes calldata payload) external;
}


